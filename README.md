# Simple Calculator

A dependency-free Python command-line calculator supporting addition, subtraction, multiplication, division, and remainder operations.

## Run

```bash
python SimpleCalc.py
```

The application validates numeric input, unsupported operations, and division by zero.

## Test

```bash
python -m unittest discover -s tests
```
